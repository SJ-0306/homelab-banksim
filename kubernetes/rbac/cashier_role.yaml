apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  namespace: staff
  name: cashier-staff-role
rules:
  - apiGroups: [""] # "" indicates the core API group
    resources: ["pods", "services"]
    verbs: ["get", "list"]
